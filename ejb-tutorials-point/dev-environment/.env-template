# ----------------------------------------------------------------------------------------------------------------------
# Purpose:
# This file serves as a template for environment variables required to configure the Docker containers
# for the Tutorials Point EJB Tutorial. It defines default values for WildFly and PostgreSQL services
# used in the development environment.
#
# Instructions:
# 1. Copy this file and rename the copy to `.env` in the same directory.
# 2. Edit the `.env` file to customize environment variables as needed.
# 3. The `.env` file is ignored by version control via `.gitignore` and should
#    not be committed to the repository.
# ----------------------------------------------------------------------------------------------------------------------

WILDFLY_CONTAINER_NAME=ejb-wildfly
WILDFLY_HTTP_PORT=8086
WILDFLY_MANAGEMENT_PORT=9996
WILDFLY_PASSWORD=developer
WILDFLY_POSTGRES_DRIVER=postgresql-42.7.6.jar
WILDFLY_ADMIN_USERNAME=developer
WILDFLY_APP_USERNAME=application
WILDFLY_VERSION=37.0.0.Final-jdk17

# PostgreSQL Configuration
POSTGRES_CONTAINER_NAME=ejb-postgres
POSTGRES_DB=ejb_database
POSTGRES_PASSWORD=developer
POSTGRES_PORT=5436
POSTGRES_USER=developer
POSTGRES_VERSION=17
